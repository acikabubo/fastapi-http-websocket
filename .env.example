# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it's in .gitignore

# ============================================
# DEBUG SETTINGS (For local development/testing)
# ============================================
# After creating your Keycloak user, add credentials here for convenient testing:
# DEBUG_AUTH=true
# DEBUG_AUTH_USERNAME=your-keycloak-username
# DEBUG_AUTH_PASSWORD=your-keycloak-password

# IMPORTANT: Set ENVIRONMENT=production to prevent DEBUG_AUTH in production
# ENVIRONMENT=production  # This will prevent DEBUG_AUTH from being enabled
